# Core
numpy
Pillow
pyyaml
tqdm
scipy
scikit-learn

# Geospatial (optional but recommended for GeoTIFF support)
rasterio

# --- Model backends (install the one you need) ---

# FLAIR-HUB (UPerNet + Swin-Tiny from HuggingFace)
segmentation-models-pytorch>=0.4.0,<0.5.0
timm>=0.9.0
huggingface-hub>=0.16.0
safetensors>=0.3.0

# LangSAM (recommended starting point — zero-shot, no labels)
# pip install git+https://github.com/luca-medeiros/lang-segment-anything.git

# SAM2 (Meta)
# pip install git+https://github.com/facebookresearch/segment-anything-2.git
# Then download a checkpoint from: https://ai.meta.com/sam2/

# PyTorch — install separately from pytorch.org for your platform
# For AMD GPU on Linux (ROCm): https://pytorch.org/get-started/locally/
# For CPU-only: pip install torch torchvision
